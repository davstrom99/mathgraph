\begin{equation}
V=\operatorname{render}(G_{\mathrm{mg}},L_{\mathrm{SCC}},B)
\label{deriv:render-graph-view}
\end{equation}

The viewer first condenses strongly connected components of the mathgraph \(G_{\mathrm{mg}}\), lays out the condensed DAG, and then draws each node using measured title-box geometry \(B\) so labels do not overlap the dependency structure.
